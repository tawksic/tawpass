[package]
name = "tawpass"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"  # Assuming you already added hex for encoding
termios = "0.3"
base64 = "0.13"